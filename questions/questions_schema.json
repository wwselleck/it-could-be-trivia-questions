{
  "$schema": "http://json-schema.org/draft-07/schema",
  "description": "A product in the catalog",
  "type": "object",
  "properties": {
      "questions": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/question"
          }
      }
  },
  "required": ["questions"],
  "definitions": {
      "question": {
          "type": "object",
          "properties": {
              "question_type_id": {
                  "type": "string"
              }
          },
          "required": ["question_type_id"]
      }
  }
}
